<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    {{>head}}
</head>
<body>
    {{>navigation}}
    <div class="container p-4">
        <div class="row">
            <div class="col-md-6">
            {{> messages}}
            {{> errors}}
            {{{ body }}}
            </div>

            {{#if show_counting}}
            <div class="col-md-6" style="max-height: 100vh; overflow-y: auto;">
                <div class="segmentation-scroll">
                {{> segmentation }}
                </div>
            </div>
            {{else}}
            <!-- Full right half + center its contents -->
            <div class="col-md-6 d-flex justify-content-center" style="max-height: 80vh; overflow-y: auto;">
                <!-- Optional max width so two 36rem cards + gap fit nicely -->
                <div class="w-100 mt-4 ps-10" style="max-width: 80rem;">
                {{> menu }}
                </div>
            </div>
            {{/if}}
        </div>
    </div>


    {{>footer}}
    <!-- Bootstrap CSS -->
        <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">

        <!-- Bootstrap JS and dependencies -->
        <script src="https://code.jquery.com/jquery-3.5.1.js" integrity="sha256-QWo7LDvxbWT2tbbQ97B53yJnYU3WhH/C8ycbRAkjPDc=" crossorigin="anonymous"></script>
        <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.1/dist/umd/popper.min.js"></script>
        <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>

  <!--CUSTOM CODE-->
  <script type="text/javascript" src="/public/js/imageMapResizer.min.js"></script>
  <script type="text/javascript">$('map').imageMapResize();</script>
</body>
</html>